
<%= li_for_item(item) do %>
  <% if !item.id.nil? %>
    <div class="view">
      
      <%= form_for_item_status(item) %>
      <label for=""><%= item.description %></label>
    </div>
  <% end %>
<% end %>
